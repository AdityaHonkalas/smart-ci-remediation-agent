Prompt -1 : Code implementation for all stages in log collection and data pipeline 

Generate code implementation for below stages:

1. Extracting logs from GitHub Actions Workflow - Extract the logs from the failing workflows runs for "https://github.com/kubernetes/kubernetes" opensource repository under /smart-ci-remediation-agent/scripts/log-collector.py.

2. Store the failure logs on local file system in the .zip files and preserve the metadata in index.json file under the /smart-ci-remediation-agent/data path

3. Log pre-processing pipeline - Implement pre-processing pipeline that includes decompression and decoding the log data from the .zip files, structural parsing, noise filtering by removing the timstamps, debug verbosity statements, error signal extraction. Use the necessary regex patterns to extract the error patterns for extracting different error codes to build the knowledge graph.

4. Build knowledge graph for the extracted error patterns by indexing with error codes, statuses, error types.

5. Build the training dataset from the pre-processed log data. Goal is to produce labelled error-> error class pairs for training a model.

6. Store the log data into the vectorDB for sematic retrieval.


Modification in the current log collector and datapre-processing:

Do the below modification for existing log-collector and pre-processing code implmentation:

1. Use https://www.kaggle.com/datasets/mirzayasirabdullah07/cicd-pipeline-failure-logs-dataset-for-aiops CI/CD failure dataset.

2. Modify the code in scripts/log-collector.py to extract the log from the user provided GitHub Action workflow run URL https://github.com/{OWNER}/{REPO}/actions/runs/{run_id}.
   Add OR modify python functions for:
   - Fetching the CI logs by calling the GitHub REST API
   - Archiving the downloaded log file into .zip and store metadata for path file in json format under the data/

3. Add the regex pattern for extracting the failure blocks while pre-processing the logs.

4. Modify the code in scripts/pre-processing-pipeline.py for:
   - Pre-processing the CI/CD failure dataset for model training with the error specific columns e.g. failure_stage failure_type, error_code, error_message, severity for training a model
   - Creating a knowledge graph for error codes, singals and error types and save in json files that can be used for filtering the logs to extract the error blocks
   - Pre-processing pipeline functions for extracting the failure block from the collected log by decompressing .zip file, decoding, removing debug statements/ verbosity lines, applying the regex patterns and knowledge graph for filtering the error specific data
 
5. Add code for model training for model anthropic.claude-sonnet-4

6. Add code for flask app, UI page for user input and returning the RCA output from model


# Add the missing requirements Prompt

Analyse the existing code and add the code changes to address the below missing things in the existing code implementation:

1. Add the code for downloading the CI/CD failure dataset from kaggle_path:"mirzayasirabdullah07/cicd-pipeline-failure-logs-dataset-for-aiops" in scripts/train-claude-sonnet.py befor model training if not present

2. Add the .env file for storing the model API key (sk-b1e8bd06bce84f2ebd48cc) for using anthropic.claude-sonnet-4 model and GitHub access token.

3. Store the regex patterns, different error codes and signals in a file to extract the failure blocks from the collected log

4. Train the model over CI/CD failure dataset for 5 iterations

5. Check if the pre-processing pipeline is fine for pre-processing failed CI log file
 
6. Add the template for UI page for the flask app

7. Check the code is perfect for below flow:

Training model: Download the CI/CD failure dataset -> Train model over the CI/CD failure dataset with failure/ error columns for 5 epochs -> Populate the error knowledge graph

Test model/ user interaction: Take the failed workflow run URL from the user -> Fetch the workflow log by calling the GitHub REST API -> save the log file in .zip on local file system -> Pre-process the log data to extract the failure block -> Supply the error block to the model for RCA -> Return the RCA report to the user   